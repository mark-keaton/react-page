---
name: Node LTS + CI Matrix
status: open
created: 2026-01-09T18:08:06Z
updated: 2026-01-09T18:08:06Z
github: [Will be updated when synced to GitHub]
depends_on: [6]
parallel: false
conflicts_with: []
---

# Task: Node LTS + CI Matrix

## Description

Validate compatibility with Node.js LTS versions (20, 22, 24) and update CI to test against all supported versions. Update package.json engines field to reflect supported Node versions.

## Acceptance Criteria

- [ ] Builds and tests pass on Node 20
- [ ] Builds and tests pass on Node 22
- [ ] Builds and tests pass on Node 24
- [ ] GitHub Actions tests all three Node versions
- [ ] package.json engines field updated
- [ ] npm audit shows 0 high/critical vulnerabilities

## Technical Details

### Update CI Matrix
```yaml
# .github/workflows/test.yml
jobs:
  jest:
    strategy:
      matrix:
        node-version: [20, 22, 24]
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
```

### Update package.json
```json
{
  "engines": {
    "node": ">=20"
  }
}
```

### Security Audit
```bash
npm audit
npm audit fix
```

Address any remaining vulnerabilities manually.

### Files Affected
- .github/workflows/test.yml
- .github/workflows/release.yaml
- package.json (engines field)

### Validation Steps
1. Test locally with nvm:
   ```bash
   nvm use 20 && yarn test
   nvm use 22 && yarn test
   nvm use 24 && yarn test
   ```
2. Push and verify CI matrix runs

## Dependencies

- [ ] Task 6: MUI + Emotion Updates (all deps updated before Node validation)

## Effort Estimate

- Size: S
- Hours: 2-3
- Parallel: false

## Definition of Done

- [ ] CI runs on Node 20, 22, 24
- [ ] All tests pass on all Node versions
- [ ] engines field set to >=20
- [ ] npm audit clean (0 high/critical)
- [ ] Documentation updated if needed
